"""
Общие исключения пакета. Модуль без импортов: его берут и ядро, и `yaseo.geo`,
не создавая кольца.
"""
from __future__ import annotations


class UpstreamError(RuntimeError):
    """Источник данных (Яндекс, ИИ-провайдер, проверяемый сайт) не отдал данных,
    и инструмент не смог выполнить работу целиком.

    Текст — человеческий, с причиной отказа источника. MCP-сервер отдаёт его
    клиенту с `isError: true`: строка «ОШИБКА …» без этого признака читается
    агентом как обычный успешный ответ.

    Частичный успех (часть запросов пакета не снялась) — не этот случай:
    инструмент отвечает как обычно и перечисляет упавшие.
    """
